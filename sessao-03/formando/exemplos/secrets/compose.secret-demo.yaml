services:
  demo:
    image: alpine:3.20
    command: ["sh", "-c", "ls -l /run/secrets && wc -c /run/secrets/demo_secret && sleep 60"]
    secrets:
      - demo_secret

secrets:
  demo_secret:
    file: ./demo_secret.txt
