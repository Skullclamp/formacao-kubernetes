spec:
  template:
    spec:
      containers:
        - name: symfony
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
