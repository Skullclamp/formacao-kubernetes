replicaCount: 2

image:
  repository: registry.invalid/s78/symfony-demo
  tag: "1.0.0"
  pullPolicy: Always

app:
  environment: prod
  version: "1.0.0-broken"
  existingSecret: postgres-credentials

probes:
  readinessPath: /ready
  livenessPath: /health
