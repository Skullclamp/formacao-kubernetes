apiVersion: v1
kind: Service
metadata:
  name: symfony
  namespace: lab-admin
spec:
  type: ClusterIP
  selector:
    app: symfony
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
